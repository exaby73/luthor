# Single Value Validators

Single Value Validators

```dart
import 'package:luthor/luthor.dart';

void main() {
  final emailValidator = l.string().email();
  print(emailValidator.validateValue("sample@example.com"));
}
```
