# Luthor

## What is luthor?

`luthor` is a validation library, inspired by [Zod](https://zod.dev). It's a pure Dart validation library,
which supports code generation with [`freezed`](https://pub.dev/packages/freezed) and
[`json_serializable`](https://pub.dev/packages/json_serializable).