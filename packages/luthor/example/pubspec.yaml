name: luthor_example
description: An example package for luthor
version: 1.0.0
publish_to: none
resolution: workspace

environment:
  sdk: '>=3.8.0 <4.0.0'

dependencies:
  luthor:

dev_dependencies:
  lints: ^6.0.0
