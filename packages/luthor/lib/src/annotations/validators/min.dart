class HasMin {
  final String? message;
  final int min;

  const HasMin(this.min, {this.message});
}

class HasMinDouble {
  final String? message;
  final double min;

  const HasMinDouble(this.min, {this.message});
}

class HasMinNumber {
  final String? message;
  final num min;

  const HasMinNumber(this.min, {this.message});
}
