class HasMax {
  final String? message;
  final int max;

  const HasMax(this.max, {this.message});
}

class HasMaxDouble {
  final String? message;
  final double max;

  const HasMaxDouble(this.max, {this.message});
}

class HasMaxNumber {
  final String? message;
  final num max;

  const HasMaxNumber(this.max, {this.message});
}
