name: luthor_generator_example
description: A sample command-line application.
version: 0.18.0
publish_to: none
resolution: workspace

environment:
  sdk: '>=3.8.0 <4.0.0'

dependencies:
  dart_mappable: ^4.8.0 # Configured via catalog
  freezed_annotation: ^3.1.0 # Configured via catalog
  json_annotation: ^4.12.0 # Configured via catalog
  luthor: 0.18.0 # Configured via catalog

dev_dependencies:
  luthor_generator: 0.18.0 # Configured via catalog
  lints: ^6.0.0 # Configured via catalog
  test: ^1.31.0 # Configured via catalog
  build_runner: ^2.15.0 # Configured via catalog
  freezed: ^3.2.0 # Configured via catalog
  json_serializable: ^6.14.0 # Configured via catalog
  dart_mappable_builder: ">=4.7.0 <4.9.0" # Configured via catalog
