name: luthor_generator
description: Generate luthor schemas
version: 0.18.0
repository: https://github.com/exaby73/luthor/tree/main/packages/luthor_generator
homepage: https://luthor.ex3.dev
resolution: workspace

environment:
  sdk: ">=3.8.0 <4.0.0"

dependencies:
  analyzer: ^10.0.0 # Configured via catalog
  build: ^4.0.0 # Configured via catalog
  collection: ^1.19.0 # Configured via catalog
  dart_mappable: ^4.8.0 # Configured via catalog
  freezed_annotation: ^3.1.0 # Configured via catalog
  json_annotation: ^4.12.0 # Configured via catalog
  luthor: 0.18.0 # Configured via catalog
  source_gen: ^4.2.0 # Configured via catalog

dev_dependencies:
  build_test: ^3.5.0 # Configured via catalog
  lint: ^2.8.0 # Configured via catalog
  test: ^1.31.0 # Configured via catalog
